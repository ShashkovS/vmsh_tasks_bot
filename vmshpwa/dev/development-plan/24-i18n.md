# Интернационализация: русский → английский

Сквозной план, дополняющий фазы 1–11. Решение и мотивация — [ADR 0004](../../../adr/0004-pwa-internationalization.md); правила для разработчиков — [docs/i18n.md](../../docs/i18n.md); термины — [глоссарий](../../docs/i18n-glossary.md).

## Результат

Весь интерфейс Student, Family, Staff и Landing, серверные сообщения PWA API и Web Push доступны на английском. Язык выбирается в кабинете, хранится в аккаунте и по умолчанию русский. Каждая фаза выкатывается отдельно; английский виден всем с P1, непереведённые места показываются по-русски.

Вне объёма: Telegram-бот (включая сообщения, которые PWA отправляет через Telegram), контент уроков (условия, скомпилированные «Задача N / Ответ / Решение»), новости, названия курсов и групп, legacy-дашборды, форматы импорта XLSX/TSV и JSON legacy print, имя установленного приложения в manifest.

## Архитектура (кратко)

- Frontend: Lingui 6, русский текст в макросах служит ключом. Каталоги `src/locales/{ru,en}.po` у каждого пакета и приложения ([lingui.config.ts](../../lingui.config.ts)); приложение сливает их в `src/i18n/catalog-<locale>.ts`, по одному чанку на локаль.
- Сборка: [vite-i18n.ts](../../vite-i18n.ts) — компиляция `.po`, Babel-макросы только для файлов с макросами, preload русского каталога, guard «ID из кода есть в каталогах».
- Runtime: [`@vmsh/i18n`](../../packages/i18n/src/index.ts) — `bootstrapLocale` до первого рендера, `LocaleProvider` снаружи всех экранов, cookie устройства `vmsh-locale`, кешированные `Intl`-форматтеры.
- Backend: [helpers/pwa/i18n.py](../../../helpers/pwa/i18n.py) и [helpers/pwa/locales/en.po](../../../helpers/pwa/locales/en.po); `pwa_error_middleware` переводит `PwaApiError.message` на язык cookie.
- Области перевода: [i18n-scopes.json](../../i18n-scopes.json) — ESLint запрещает там неупакованную кириллицу, `make pwa-i18n-check` требует английский.

## Производительность (бюджет каждой фазы)

- Начальный JS (entry + modulepreload, brotli): не более +10 KB к baseline P0 на приложение.
- Каталог грузится параллельно с entry (preload), без последовательного запроса.
- Медиана FCP (cold и warm, `scripts/startup-benchmark.mjs`): не хуже baseline более чем на 30 мс.
- `pnpm build` и `pnpm test`: не более +25% к baseline P0.
- Горячие списки (live marking, очередь проверки, справочник) — без `<Trans>` в каждой ячейке; форматтеры только кешированные.

Замеры: `node scripts/bundle-report.mjs`, `node scripts/startup-benchmark.mjs`; отчёт — [i18n-performance-report.md](../i18n-performance-report.md).

## Фазы

| Фаза | Объём                                                                                                                                               | Результат для пользователя                         |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| P0   | Инфраструктура frontend и backend, гейты, замеры                                                                                                    | Изменений нет                                      |
| P1   | `auth_accounts.locale`, `PUT auth/locale`, переключатель (профили Student/Family, шапка Staff), оболочка, вход, общие ошибки                        | English доступен всем; каркас и вход на английском |
| P2   | Family + общие компоненты прогресса, уведомлений, организаторов; Web Push на языке получателя                                                       | Родитель полностью на английском                   |
| P3   | Student + отправка ответов, письменная сдача, поддержка; перевод дефолтного feedback при чтении                                                     | Ученик полностью на английском                     |
| P4   | Staff: проверка, live marking, тестирование, результаты, статистика                                                                                 | Проверка на английском                             |
| P5   | Staff: курсы, расписание, аудитории, устный приём, новости, баннеры                                                                                 | Администрирование курсов на английском             |
| P6   | Staff: контент, уроки, импорт, синонимы, figure layout, whiteboard                                                                                  | Работа с контентом на английском                   |
| P7   | Staff: пользователи, доступы, аудит, привязки Telegram, аналитика, сводка, поддержка                                                                | Staff полностью на английском                      |
| P8   | Запрет кириллицы на весь production-код, строгое покрытие, оставшиеся сохранённые метки, двуязычный 503, Landing, полный EN-проход и итоговый отчёт | Проверенный английский интерфейс без пропусков     |

### Рецепт фазы перевода (P1–P7)

1. Обернуть строки области в макросы; модульные карты — в `msg`; плюрализаторы — в ICU `plural`; `ru-RU`-форматтеры — в `useFormatters()`.
2. `make pwa-i18n-extract`, перевести новые `msgstr` (frontend `en.po`, backend `helpers/pwa/locales/en.po`) по глоссарию.
3. Добавить glob'ы области в `i18n-scopes.json` (frontend и backend).
4. Тесты затронутых компонентов — через `renderWithI18n` из `@vmsh/test-utils/i18n`; русские проверки остаются.
5. Расширить английский e2e smoke; просмотреть истории области в Storybook с глобалом «English».
6. DoD-гейты и `make pwa-i18n-check`; замеры по бюджету; пруфы ниже; записи в оба `STATUS.md`.

## Пруфы завершения этапа

### P0 — инфраструктура

- [x] Toolchain spike: Lingui 6.7 + `@rolldown/plugin-babel` 0.2.4 + Babel 7.29 на Vite 8.1.5, TanStack autoCodeSplitting, Vitest 4.1, Storybook 10.5; `.po` из workspace-пакетов; production без исходных строк в route-чанках; guard проверен негативным кейсом.
- [x] `@vmsh/i18n`, `lingui.config.ts`, каталоги всех пакетов и приложений, подключение во всех `main.tsx`, Vitest `setupFiles`, Storybook (загрузчик, провайдер, тулбар «Язык интерфейса»).
- [x] Backend: `helpers/pwa/i18n.py`, язык запроса из cookie в `pwa_error_middleware`, перевод `PwaApiError` и ошибок WebSocket, `params` у `PwaApiError`, `vmshpwa/scripts/backend_i18n.py`, каталог из 534 сообщений.
- [x] Гейты: ESLint `lingui/*` и запрет кириллицы в областях, `make pwa-i18n-extract` / `make pwa-i18n-check` (синхронность, покрытие, полнота слияния каталогов).
- [x] Замеры: [i18n-performance-report.md](../i18n-performance-report.md) — в пределах бюджета.
- [x] Tests: `@vmsh/i18n` (13), backend i18n (18 + WebSocket на английском), `e2e/i18n.spec.ts` — английский каталог через cookie и русский по умолчанию, 6/6 в Chromium, WebKit, Firefox (`make pwa-e2e-i18n`).
- [x] Регресс против чистого `7a9ddcbc`: `e2e:auth` и `e2e:runtime-isolation` (75/75) без новых падений; полный `make pwa-e2e` — 270 passed, 49 failed, 2 flaky. Упавшие сценарии проверены на baseline выборочно (`submissions`, `content`, `family`, `auth`) и падают там так же; остальные зависят от текущей даты в seed-данных («возраст», «текущее занятие», окна устного приёма).
- Revision: коммит `feat(i18n): add RU/EN infrastructure (P0)` — первый, добавивший `vmshpwa/lingui.config.ts` (`git log --diff-filter=A -- vmshpwa/lingui.config.ts`).
- Known limitations, существовавшие до P0 и не связанные с i18n: 9 замечаний ESLint; `packages/contracts/src/content-api.test.ts` (нет `testRechecks` в фикстуре); 10 тестов `pwa_tests`; нестабильные истории Storybook (на baseline 35–56 падений, на P0 стабильно 14 из того же набора); перечисленные e2e-сценарии.

### P1 — язык в аккаунте, переключатель и оболочка

- [ ] Миграция `auth_accounts.locale` + rollback + `make pwa-schema-update`.
- [ ] `principal.locale`, `PUT /{audience}/api/v1/auth/locale`, контракт.
- [ ] Переключатель, синхронизация между устройствами, оболочка и вход переведены.
- [ ] `e2e/i18n.spec.ts` в 3 браузерах.

### P2–P8

Заполняются по мере выполнения по рецепту выше.

import { messages as ui } from '@vmsh/ui/locales/en.po'

import { messages as landing } from '../locales/en.po'

/** English catalog of the Landing app and every `@vmsh/*` package it uses. */
export const messages = { ...ui, ...landing }

# Печать из админки — статус

2026-09-12: владелец изменил ближайший объём работ — token-protected read-only
API, эмулирующий результат `parse_xls_conduit`, для ближайшего понедельника.
Реализация локально завершена; полный print UI отложен. По уточнению владельца
внешние скрипты не редактировались: подготовлены
[подробная инструкция](legacy-api.md) и [клиент для копирования](legacy_print_client.py).
По уточнению владельца a11 сам обновляет снимок при каждом запуске; отдельные
терминальные команды `events`/`download` нужны только для диагностики. Следующее
уточнение убрало еженедельную настройку event ID: a11 автоматически выбирает
единственное событие с confirmed plan и набором номеров занятий `[cur_les]`.
Явный `ipe-...` остаётся аварийным выбором при неоднозначности.

- API возвращает логин в `ID`/`IDd`, внутренний `UserID` для Code128 и `GroupID`
  для a13. Выдаётся один подтверждённый план выбранного события/номера занятия.
- Новые HTTP/SQLite проверки, копируемый клиент и конфигурация: **23 PASS**.
  Ruff и проверка diff/ссылок/JSON-шаблона прошли.
- Существующий classroom HTTP suite: **4 PASS, 1 FAIL**. Оставшийся тест
  `test_admin_materializes_updates_and_confirms_classroom_layout` ожидает
  9 категорий уведомлений вместо фактических 10. Такое же падение воспроизведено
  в отдельной чистой копии HEAD `57872ad0`, без нового API. Оно не исправлялось
  в рамках печати.
- Production проверен только на чтение: backend активен, checkout чистый,
  ветка `vmshpwa`, ревизия `57872ad0c27c9a75c6bbb9cd1387a2b4ba22c8c6`.
  Код и токен на сервер ещё не доставлены; live API не объявляется готовым.
- Штатный `vmshbegetagent` может писать checkout, но не production JSON
  конфигурации. У него нет sudo согласно [SSH guide](../../ssh/AGENTS.md).
  Для включения нужен владелец/разрешённый привилегированный deployment:
  добавить отдельный `legacy_print_api_token`, выпустить код, перезапустить
  backend и выполнить authenticated HTTP smoke без вывода персональных данных.

## Первоначальное исследование (до изменения объёма работ)

2026-09-12: завершено первичное исследование по запросу владельца.

- Изучены `a11`–`a14`, `z_helpers`, настройки, четыре предоставленных
  PDF, четыре TeX-шаблона, соответствующая часть `newlistok.sty` и структура Excel.
- Сопоставлены существующие classroom/content/result/attendance источники портала.
- [Наблюдения, варианты и вопросы](analysis-2026-09-12.md) сохранены для продолжения.
- Реализация, выбор движка и изменение операционного процесса пока не согласованы.
- Открыты вопросы совместимости распознавания, состава комплектов, тиражей,
  прошлого занятия, публикации материалов и момента фиксации данных.
- Продуктовый код и существующие планы других работ не менялись. Legacy-скрипты
  не запускались; исходные документы и базы не изменялись. Тесты приложения
  не запускались: проверялись документы, структура файлов и исходный код.

Вопросы полного печатного раздела отложены. Ближайший следующий шаг — включить
API на production, перенести клиент и небольшие правки по инструкции, скачать
один снимок события и проверить полный комплект одной аудитории.
